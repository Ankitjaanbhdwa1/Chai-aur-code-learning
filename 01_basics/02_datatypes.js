"use strict"; // treat all js as a new java version

//alert(3 + 3)//we are using nodejs, not browser

console.log(3 
    + 
    3); // code readability should be high//

console.log("Amarjeet");

let name = "Amarjeet"
let age = 18
let isloggedIn = false

//*premitive data types*//

//number => 2 to power 53, its a range
//bigint => it uses with big range like facebook reddit
//string => ""
//boolean => true/false
//null => its a standalone value, it a type(empty)
//undefined => value doesn't defined, variable is declared but and value does't assign
//symbol =>  for uses uniqueness,used by figma tools for defining which is individual component, mostly uses for uniquness

//object(null)

console.log(typeof undefined);//undefined, because it is a type
 console.log(typeof null);//object, because its type is object 